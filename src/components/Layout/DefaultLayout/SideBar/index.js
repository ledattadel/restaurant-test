import * as React from 'react';
import { Layout } from 'antd';
const { Sider } = Layout;

const Sidebar = () => {
    return (
        <Sider>SideBarr</Sider>
    );
}

export default Sidebar;
